\usepackage{minted}
\usepackage{xcolor} % to access the named colour LightGray

% A decent default style for all minted blocks
\setminted{
  fontsize=\small,
  breaklines=true,
  linenos=true,
  autogobble=true,     % removes common indentation
  tabsize=2,
  frame=lines,         % simple top/bottom rule
  framesep=2mm
}
